
export const nodeControllDevice = ({ id }) => `_node_${id}_controll_device_`; // id for node

export const clientPayloadSensor = ({ id }) => `_client_${id}_payload_sensor_`; // id for client(USER)

export const clientResponseDevice = ({ id }) => `_client_${id}_response_device_`; // id for client(USER)

export const clientStatusNode = ({ id }) => `_client_${id}_status_node_`; // id for client(USER)
